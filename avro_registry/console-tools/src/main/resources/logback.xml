<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- %L should be removed eventually for performance -->
            <pattern>%d{ISO8601} %-5level[%X{akkaSource}] %logger{26}:%L - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="io.netty" level="INFO" />
    <logger name="akka" level="${LOG_LEVEL:-INFO}" />
    <logger name="org.apache.zookeeper" level="WARN" />
    <logger name="org.I0Itec" level="WARN" />
    <logger name="org.apache.kafka" level="WARN" />
    <logger name="kafka" level="${LOG_LEVEL:-WARN}"/>
    <logger name="cakesolutions" level="WARN" />
    <logger name="com.stuart" level="${LOG_LEVEL:-INFO}" />

    <root level="ERROR">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
